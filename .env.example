# App
PORT=3000
NODE_ENV=development
JWT_SECRET=change_me_please

# Database (use 'mysql' as host when running with Docker Compose)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=agenda
DB_USER=root
DB_PASS=secret
